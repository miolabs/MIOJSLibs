enum MIOCoreEventKey{
    
    Enter = 13,
    Escape = 27,
    ArrowLeft = 37,
    ArrowUp = 38,
    ArrowRight = 39,
    ArrowDown = 40
}

enum MIOCoreEventType{

    KeyUp,
    KeyDown,
    MouseUp,
    MouseDown
}

let _mio_core_events = ["keyup", "keydown", "mouseup", "mousedown"];


